@model IEnumerable<BookDTO_client>

@if (Model.Count() == 0)
{
    <h1>No Books available.</h1>
}
else
{
<option value="">-- Select a book --</option>

@foreach (var book in Model)
{
    <option value="@book.Title">@book.Title</option>
}
}